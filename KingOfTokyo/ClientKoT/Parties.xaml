<Page x:Class="ClientKoT.Parties"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ClientKoT"
      xmlns:reseau="clr-namespace:Client.Reseau"
      mc:Ignorable="d" 
      d:DesignHeight="768" d:DesignWidth="1024"
      Title="Parties">

    <Grid>
        <Rectangle>
                <Rectangle.Fill>
                    <ImageBrush ImageSource="images/santa_pepe.png" />
                    <!-- Background à changer ici avec image definitive -->
                </Rectangle.Fill>
        </Rectangle>

            <!-- Liste -->
        <TextBlock Height="20" HorizontalAlignment="Center" Margin="0,30,0,0" Name="ListePartiesHeader" Text="Liste des parties sur le serveur :" VerticalAlignment="Top" FontSize="17" FontStretch="ExtraCondensed"/>
        <Button Content="Créer une partie" Height="20" HorizontalAlignment="Center" Margin="0,80,0,0" Name="buttonCréerPartie" VerticalAlignment="Top" Width="100" Click="ButtonCreerPartie_Click"/>

        <!--<ListView HorizontalAlignment="Center" Margin="0,80,0,0" Height="300" Width="600" ItemsSource="{Binding reseau:HelperServeur ReadListeParties}">
            <ListView.View HorizontalAlignment="Center" Margin="0,200,0,0" Name="vueListe" VerticalAlignment="top">
                <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Nom">
                    <GridViewColumn DisplayMemberBinding="{Binding reseau:HelperServeur ReadListeParties.NbJoueurs}" Header="Nb Joueurs" Width="200"/> A FIX avec nom réel
                    <GridViewColumn DisplayMemberBinding="{Binding reseau:HelperServeur ReadListeParties.Nom}" Width="200">  A FIX avec nom réel
                        <GridViewColumnHeader>Nom
                            <GridViewColumnHeader.ContextMenu>
                                <ContextMenu  MenuItem.Click="NomCM_Click" Name="NomCM">
                                    <MenuItem Header="Ascending" />
                                    <MenuItem Header="Descending" />
                                </ContextMenu>
                            </GridViewColumnHeader.ContextMenu>
                        </GridViewColumnHeader>
                    </GridViewColumn>
                    <GridViewColumn Header="Action" Width="200"/>Aucune idée comment insérer un btn rejoindre par ligne de la liste. Il faut link les btn avec les id partie
                </GridView>
            </ListView.View>
        </ListView> -->
        <ListView HorizontalAlignment="Center" Margin="0,80,0,0" Height="300" Width="600"><!-- A FIX avec la source de data dans la balise ListView::> ItemsSource="{Binding Source={reseau:HelperServeur ListeParties }}"-->
            <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Nom">
                <GridViewColumn DisplayMemberBinding="{Binding Path=NbJoueurs}" Header="Nb Joueurs" Width="200"/>
                <!-- A FIX avec nom réel-->
                <GridViewColumn DisplayMemberBinding="{Binding Path=NomPartie}" Width="200">
                    <!-- A FIX avec nom réel-->
                    <GridViewColumnHeader>Nom
                        <GridViewColumnHeader.ContextMenu>
                            <ContextMenu  MenuItem.Click="NomCM_Click" Name="NomCM">
                                <MenuItem Header="Ascending" />
                                <MenuItem Header="Descending" />
                            </ContextMenu>
                        </GridViewColumnHeader.ContextMenu>
                    </GridViewColumnHeader>
                </GridViewColumn>
                <GridViewColumn Header="Action" Width="200"/>
                <!--Aucune idée comment insérer un btn rejoindre par ligne de la liste. Il faut link les btn avec les id partie-->
            </GridView>
=======
        <ListView HorizontalAlignment="Center" Margin="0,80,0,0" Height="300" Width="600"><!-- A FIX avec la source de data dans la balise ListView::> ItemsSource="{Binding Source={reseau:HelperServeur ListeParties }}"-->
            <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Nom">
                <GridViewColumn DisplayMemberBinding="{Binding Path=NbJoueurs}" Header="Nb Joueurs" Width="200"/>
                <!-- A FIX avec nom réel-->
                <GridViewColumn DisplayMemberBinding="{Binding Path=NomPartie}" Width="200">
                    <!-- A FIX avec nom réel-->
                    <GridViewColumnHeader>Nom
                        <GridViewColumnHeader.ContextMenu>
                            <ContextMenu  MenuItem.Click="NomCM_Click" Name="NomCM">
                                <MenuItem Header="Ascending" />
                                <MenuItem Header="Descending" />
                            </ContextMenu>
                        </GridViewColumnHeader.ContextMenu>
                    </GridViewColumnHeader>
                </GridViewColumn>
                <GridViewColumn Header="Action" Width="200"/>
                <!--Aucune idée comment insérer un btn rejoindre par ligne de la liste. Il faut link les btn avec les id partie-->
            </GridView>
>>>>>>> Debut tcp
||||||| merged common ancestors
=======
||||||| merged common ancestors
        <ListView HorizontalAlignment="Center" Margin="0,80,0,0" Height="300" Width="600"><!-- A FIX avec la source de data dans la balise ListView::> ItemsSource="{Binding Source={reseau:HelperServeur ListeParties }}"-->
            <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Nom">
                <GridViewColumn DisplayMemberBinding="{Binding Path=NbJoueurs}" Header="Nb Joueurs" Width="200"/>
                <!-- A FIX avec nom réel-->
                <GridViewColumn DisplayMemberBinding="{Binding Path=NomPartie}" Width="200">
                    <!-- A FIX avec nom réel-->
                    <GridViewColumnHeader>Nom
                        <GridViewColumnHeader.ContextMenu>
                            <ContextMenu  MenuItem.Click="NomCM_Click" Name="NomCM">
                                <MenuItem Header="Ascending" />
                                <MenuItem Header="Descending" />
                            </ContextMenu>
                        </GridViewColumnHeader.ContextMenu>
                    </GridViewColumnHeader>
                </GridViewColumn>
                <GridViewColumn Header="Action" Width="200"/>
                <!--Aucune idée comment insérer un btn rejoindre par ligne de la liste. Il faut link les btn avec les id partie-->
            </GridView>
>>>>>>> fb53fe27883a94ebc5ff8b51c32bdc0e11ef7eec
        </ListView>
    </Grid>
</Page>
